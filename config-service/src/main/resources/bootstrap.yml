server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeffreymaestre/microservicios.git
          searchPath: config-data
          username: ${GUT_USER}
          password: ${GIT_PASSWORD}

security:
  user:
    name: root
    password: s3cr3t
